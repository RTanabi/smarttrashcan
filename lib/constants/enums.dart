enum StorageKeys {
  lang,
  theme,
}

enum AppTextFontSize {
  xxSmall, // 3x small
  xSmall, // 2x small
  small, // s
  normal, // normal
  medium, // medium
  large, // 1x large
  xLarge, // 2x large
  xxLarge, // 3x large
}

enum AppTextFontWeight {
  ulLight, // 2x light
  light, // light
  medium, // medium
  bold, // bold
  heavy, // 2x bold
}

enum ButtonVariant {
  contained,
  outlined,
}

enum ButtonSize {
  large,
  medium,
  small,
  xSmall,
}

enum IconButtonSize {
  large,
  medium,
  small,
  xSmall,
}

enum AlertType {
  success,
  danger,
  info,
  warning,
}

enum ScreenScaffoldLeadingType {
  back,
  close,
}

enum VerifyType {
  verifyAccount,
  unlockAccount,
}

enum PriceTextTypeHandler {
  priceBase,
  signBase,
}

enum CarouselNavigationDirection {
  next,
  previous,
}
